<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.seconddiabetesapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".Onboarding.OnboardingStart"></activity>
        <activity android:name=".Onboarding.SignupFirst" />
        <activity android:name=".Onboarding.SignupSecond" />
        <activity android:name=".Onboarding.Login" />
        <activity android:name=".Onboarding.OnboardingMainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Onboarding.SignupThird"></activity>
    </application>

</manifest>